<nav class="medkit-tabs tabs">
  {{#each tabs}}
  <a class="item {{cssClass}}" data-action="tab" data-tab="{{@key}}" data-group="main" data-tooltip="{{localize tooltip}}">
    <i class="{{icon}}"></i>
    {{localize label}}
  </a>
  {{/each}}
</nav>